# Teleparty — Home revamp

Open `index.html` over HTTP (the playground blocks `file://`).

| File | |
|---|---|
| `index.html` | Home (default). Profile is the dock tab on this phone. |
| `party.html` | Party |
| `components.html` | Tokens and components |
